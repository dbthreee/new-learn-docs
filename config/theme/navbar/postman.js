import { createNavbarGroup } from '../../../utils/navbar_utils';

const navbar_postman = createNavbarGroup({
  text: 'Postman',
  link: '/default/postman/',
  // children: []
});

export default navbar_postman;
